const bouncer = (age) =>{
  if (age >= 21) {
    console.log('DRINK (at the bar')
  } else {
    console.log('Shame');
  }
  };
bouncer(12);
bouncer(93);

const me = 'martin';
console.log(me.length);
